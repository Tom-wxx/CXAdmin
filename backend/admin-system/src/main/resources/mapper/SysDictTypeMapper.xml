<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysDictTypeMapper">

    <resultMap type="com.admin.system.entity.SysDictType" id="SysDictTypeResult">
        <id property="dictId" column="dict_id"/>
        <result property="dictName" column="dict_name"/>
        <result property="dictType" column="dict_type"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectDictTypeVo">
        SELECT dict_id, dict_name, dict_type, status, create_by, create_time, update_by, update_time, remark
        FROM sys_dict_type
    </sql>

    <select id="selectDictTypePage" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        WHERE deleted = 0
        <if test="dictName != null and dictName != ''">
            AND dict_name LIKE CONCAT('%', #{dictName}, '%')
        </if>
        <if test="dictType != null and dictType != ''">
            AND dict_type LIKE CONCAT('%', #{dictType}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectDictTypeList" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        WHERE deleted = 0 AND status = '0'
        ORDER BY create_time DESC
    </select>

    <select id="selectDictTypeByType" parameterType="String" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        WHERE dict_type = #{dictType} AND deleted = 0
    </select>

    <select id="checkDictTypeUnique" parameterType="String" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        WHERE dict_type = #{dictType} AND deleted = 0
        LIMIT 1
    </select>

    <select id="checkDictDataExist" parameterType="String" resultType="int">
        SELECT COUNT(1) FROM sys_dict_data
        WHERE dict_type = #{dictType} AND deleted = 0
    </select>

</mapper>
