<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysDictDataMapper">

    <resultMap type="com.admin.system.entity.SysDictData" id="SysDictDataResult">
        <id property="dictCode" column="dict_code"/>
        <result property="dictSort" column="dict_sort"/>
        <result property="dictLabel" column="dict_label"/>
        <result property="dictValue" column="dict_value"/>
        <result property="dictType" column="dict_type"/>
        <result property="cssClass" column="css_class"/>
        <result property="listClass" column="list_class"/>
        <result property="isDefault" column="is_default"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectDictDataVo">
        SELECT dict_code, dict_sort, dict_label, dict_value, dict_type, css_class, list_class, is_default, status,
               create_by, create_time, update_by, update_time, remark
        FROM sys_dict_data
    </sql>

    <select id="selectDictDataPage" resultMap="SysDictDataResult">
        <include refid="selectDictDataVo"/>
        WHERE deleted = 0
        <if test="dictType != null and dictType != ''">
            AND dict_type = #{dictType}
        </if>
        <if test="dictLabel != null and dictLabel != ''">
            AND dict_label LIKE CONCAT('%', #{dictLabel}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY dict_sort ASC, create_time DESC
    </select>

    <select id="selectDictDataByType" parameterType="String" resultMap="SysDictDataResult">
        <include refid="selectDictDataVo"/>
        WHERE dict_type = #{dictType} AND status = '0' AND deleted = 0
        ORDER BY dict_sort ASC
    </select>

    <select id="selectDictDataList" resultMap="SysDictDataResult">
        <include refid="selectDictDataVo"/>
        WHERE deleted = 0
        <if test="dictData.dictType != null and dictData.dictType != ''">
            AND dict_type = #{dictData.dictType}
        </if>
        <if test="dictData.dictLabel != null and dictData.dictLabel != ''">
            AND dict_label LIKE CONCAT('%', #{dictData.dictLabel}, '%')
        </if>
        <if test="dictData.status != null and dictData.status != ''">
            AND status = #{dictData.status}
        </if>
        ORDER BY dict_sort ASC
    </select>

</mapper>
