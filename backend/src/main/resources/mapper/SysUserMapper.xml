<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysUserMapper">

    <resultMap type="com.admin.system.entity.SysUser" id="SysUserResult">
        <id property="userId" column="user_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <resultMap type="com.admin.system.vo.UserVO" id="UserVOResult">
        <id property="userId" column="user_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="phone" column="phonenumber"/>
        <result property="gender" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="loginIp" column="login_ip"/>
        <result property="loginDate" column="login_date"/>
        <result property="createTime" column="create_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectUserByUsername" parameterType="String" resultMap="SysUserResult">
        SELECT u.user_id, u.dept_id, u.username, u.nickname, u.email, u.phonenumber, u.sex, u.avatar,
               u.password, u.status, u.login_ip, u.login_date, u.create_by, u.create_time, u.remark
        FROM sys_user u
        WHERE u.username = #{username} AND u.deleted = 0
    </select>

    <select id="selectUserList" parameterType="com.admin.system.entity.SysUser" resultMap="SysUserResult">
        SELECT u.user_id, u.dept_id, u.username, u.nickname, u.email, u.phonenumber, u.sex, u.avatar,
               u.status, u.login_ip, u.login_date, u.create_by, u.create_time, u.remark
        FROM sys_user u
        WHERE u.deleted = 0
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="status != null and status != ''">
            AND u.status = #{status}
        </if>
        <if test="phonenumber != null and phonenumber != ''">
            AND u.phonenumber LIKE CONCAT('%', #{phonenumber}, '%')
        </if>
        ORDER BY u.user_id
    </select>

    <select id="checkUsernameUnique" parameterType="String" resultMap="SysUserResult">
        SELECT user_id, username
        FROM sys_user
        WHERE username = #{username} AND deleted = 0
        LIMIT 1
    </select>

    <select id="checkPhoneUnique" parameterType="String" resultMap="SysUserResult">
        SELECT user_id, phonenumber
        FROM sys_user
        WHERE phonenumber = #{phonenumber} AND deleted = 0
        LIMIT 1
    </select>

    <select id="checkEmailUnique" parameterType="String" resultMap="SysUserResult">
        SELECT user_id, email
        FROM sys_user
        WHERE email = #{email} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectUserPage" resultMap="UserVOResult">
        SELECT u.user_id, u.dept_id, u.username, u.nickname, u.email, u.phonenumber, u.sex, u.avatar,
               u.status, u.login_ip, u.login_date, u.create_time, u.remark,
               d.dept_name
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        WHERE u.deleted = 0
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND u.phonenumber LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null and status != ''">
            AND u.status = #{status}
        </if>
        ORDER BY u.create_time DESC
    </select>

    <select id="selectUserVOById" parameterType="Long" resultMap="UserVOResult">
        SELECT u.user_id, u.dept_id, u.username, u.nickname, u.email, u.phonenumber, u.sex, u.avatar,
               u.status, u.login_ip, u.login_date, u.create_time, u.remark,
               d.dept_name
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        WHERE u.user_id = #{userId} AND u.deleted = 0
    </select>

    <!-- 批量新增用户岗位信息 -->
    <insert id="batchUserPost">
        INSERT INTO sys_user_post(user_id, post_id) VALUES
        <foreach item="postId" collection="postIds" separator=",">
            (#{userId}, #{postId})
        </foreach>
    </insert>

    <!-- 删除用户和岗位关联 -->
    <delete id="deleteUserPostByUserId" parameterType="Long">
        DELETE FROM sys_user_post WHERE user_id = #{userId}
    </delete>

    <!-- 批量新增用户角色信息 -->
    <insert id="batchUserRole">
        INSERT INTO sys_user_role(user_id, role_id) VALUES
        <foreach item="roleId" collection="roleIds" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <!-- 删除用户和角色关联 -->
    <delete id="deleteUserRoleByUserId" parameterType="Long">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <!-- 根据用户ID查询岗位ID列表 -->
    <select id="selectPostIdsByUserId" parameterType="Long" resultType="Long">
        SELECT post_id FROM sys_user_post WHERE user_id = #{userId}
    </select>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" parameterType="Long" resultType="Long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
    </select>

</mapper>
