<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysPostMapper">

    <resultMap type="com.admin.system.entity.SysPost" id="SysPostResult">
        <id property="postId" column="post_id"/>
        <result property="postCode" column="post_code"/>
        <result property="postName" column="post_name"/>
        <result property="postSort" column="post_sort"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectPostVo">
        SELECT p.post_id, p.post_code, p.post_name, p.post_sort, p.status,
               p.create_by, p.create_time, p.update_by, p.update_time, p.remark
        FROM sys_post p
    </sql>

    <select id="selectPostList" parameterType="com.admin.system.entity.SysPost" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        WHERE p.deleted = 0
        <if test="postCode != null and postCode != ''">
            AND p.post_code LIKE CONCAT('%', #{postCode}, '%')
        </if>
        <if test="postName != null and postName != ''">
            AND p.post_name LIKE CONCAT('%', #{postName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND p.status = #{status}
        </if>
        ORDER BY p.post_sort
    </select>

    <select id="selectPostPage" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        WHERE p.deleted = 0
        <if test="postCode != null and postCode != ''">
            AND p.post_code LIKE CONCAT('%', #{postCode}, '%')
        </if>
        <if test="postName != null and postName != ''">
            AND p.post_name LIKE CONCAT('%', #{postName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND p.status = #{status}
        </if>
        ORDER BY p.post_sort
    </select>

    <select id="selectPostById" parameterType="Long" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        WHERE p.post_id = #{postId} AND p.deleted = 0
    </select>

    <select id="checkPostExistUser" parameterType="Long" resultType="int">
        SELECT COUNT(1) FROM sys_user_post
        WHERE post_id = #{postId}
    </select>

    <select id="checkPostNameUnique" parameterType="String" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        WHERE p.post_name = #{postName} AND p.deleted = 0
        LIMIT 1
    </select>

    <select id="checkPostCodeUnique" parameterType="String" resultMap="SysPostResult">
        <include refid="selectPostVo"/>
        WHERE p.post_code = #{postCode} AND p.deleted = 0
        LIMIT 1
    </select>

</mapper>
