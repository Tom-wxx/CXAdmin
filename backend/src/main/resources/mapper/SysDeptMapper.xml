<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysDeptMapper">

    <resultMap type="com.admin.system.entity.SysDept" id="SysDeptResult">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectDeptVo">
        SELECT d.dept_id, d.parent_id, d.ancestors, d.dept_name, d.order_num, d.leader, d.phone, d.email, d.status,
               d.create_by, d.create_time, d.update_by, d.update_time
        FROM sys_dept d
    </sql>

    <select id="selectDeptList" parameterType="com.admin.system.entity.SysDept" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        WHERE d.deleted = 0
        <if test="deptId != null and deptId != 0">
            AND d.dept_id = #{deptId}
        </if>
        <if test="parentId != null and parentId != 0">
            AND d.parent_id = #{parentId}
        </if>
        <if test="deptName != null and deptName != ''">
            AND d.dept_name LIKE CONCAT('%', #{deptName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND d.status = #{status}
        </if>
        ORDER BY d.parent_id, d.order_num
    </select>

    <select id="selectDeptById" parameterType="Long" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        WHERE d.dept_id = #{deptId} AND d.deleted = 0
    </select>

    <select id="selectChildrenDeptById" parameterType="Long" resultMap="SysDeptResult">
        SELECT * FROM sys_dept
        WHERE FIND_IN_SET(#{deptId}, ancestors) AND deleted = 0
    </select>

    <select id="selectNormalChildrenDeptById" parameterType="Long" resultType="int">
        SELECT COUNT(*) FROM sys_dept
        WHERE status = '0' AND deleted = 0 AND FIND_IN_SET(#{deptId}, ancestors)
    </select>

    <select id="hasChildByDeptId" parameterType="Long" resultType="int">
        SELECT COUNT(1) FROM sys_dept
        WHERE parent_id = #{deptId} AND deleted = 0
        LIMIT 1
    </select>

    <select id="checkDeptExistUser" parameterType="Long" resultType="int">
        SELECT COUNT(1) FROM sys_user
        WHERE dept_id = #{deptId} AND deleted = 0
    </select>

    <select id="checkDeptNameUnique" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        WHERE d.dept_name = #{deptName} AND d.parent_id = #{parentId} AND d.deleted = 0
        LIMIT 1
    </select>

    <update id="updateDeptChildren" parameterType="java.util.List">
        UPDATE sys_dept SET ancestors =
        <foreach collection="depts" item="item" index="index" separator=" " open="CASE dept_id" close="END">
            WHEN #{item.deptId} THEN #{item.ancestors}
        </foreach>
        WHERE dept_id IN
        <foreach collection="depts" item="item" index="index" separator="," open="(" close=")">
            #{item.deptId}
        </foreach>
    </update>

</mapper>
