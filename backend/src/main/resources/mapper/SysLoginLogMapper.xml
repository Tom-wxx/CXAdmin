<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.system.mapper.SysLoginLogMapper">

    <resultMap id="SysLoginLogResult" type="com.admin.system.entity.SysLoginLog">
        <id property="infoId" column="info_id"/>
        <result property="username" column="username"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="status" column="status"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
    </resultMap>

    <sql id="selectLoginLogVo">
        SELECT info_id, username, ipaddr, login_location, browser, os, status, msg, login_time
        FROM sys_login_log
    </sql>

    <select id="selectLoginLogPage" resultMap="SysLoginLogResult">
        <include refid="selectLoginLogVo"/>
        WHERE 1=1
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="ipaddr != null and ipaddr != ''">
            AND ipaddr LIKE CONCAT('%', #{ipaddr}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="beginTime != null and beginTime != ''">
            AND DATE_FORMAT(login_time,'%Y-%m-%d') &gt;= DATE_FORMAT(#{beginTime},'%Y-%m-%d')
        </if>
        <if test="endTime != null and endTime != ''">
            AND DATE_FORMAT(login_time,'%Y-%m-%d') &lt;= DATE_FORMAT(#{endTime},'%Y-%m-%d')
        </if>
        ORDER BY login_time DESC
    </select>

    <delete id="cleanLoginLog">
        TRUNCATE TABLE sys_login_log
    </delete>

</mapper>
